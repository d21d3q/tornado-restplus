language: python
python:
    - "2.7"
    - "3.5"
install:
    - pip install -r requirements.txt
    - pip install -r requirements_test.txt
# script: python -m pytest tests
script: py.test --cov-report term --cov=tornado_restplus tests
